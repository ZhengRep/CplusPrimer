#include<iostream>
#include<string>

using namespace std;

int main()
{
	//char ca[] = { 'c', '+', 'c' };
	//cout << strlen(ca) << endl; //no zero-terminated, so strlen(ca) is a random value

	return 0;
}